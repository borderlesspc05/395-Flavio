RENDER — USE ESTA CONFIG (funciona com repo na raiz)
====================================================

  Root Directory:     (DEIXE VAZIO)
  Build Command:      npm install && npm run build
  Start Command:      npm start
  Health Check Path:  /api/health

(O postbuild no package.json compila a API em server/dist automaticamente.)

Alternativa:
  Root Directory:     server
  Build Command:      npm install --include=dev && npm run build
  Start Command:      npm start

ERRADO — causa "Cannot GET /api/health":
  Start = vite preview, serve, yarn start sem npm start
  Build só frontend sem postbuild (versão antiga do repo)

URL: copie do painel Render (pode NÃO ser three95-flavio.onrender.com)

Teste: /api/health → JSON {"status":"ok"} (não HTML)
